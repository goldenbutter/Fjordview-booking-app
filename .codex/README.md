# Codex Project-Local Config

Use this directory for Codex-specific project-local files such as hooks, scratch notes, generated local state, or session-only helper config.

Only this README is intended to be tracked. The rest of `.codex/` is ignored by `.gitignore`, mirroring the repo's `.claude/` convention.

Codex sessions should still read `../_docs/AGENT-PROTOCOL.md` before work starts; this folder is not a replacement for the shared multi-agent protocol.
